apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
  labels:
    {{- include "ml-portal.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $key, $value := .Values.secrets }}
  {{- if $value }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
  {{- end }}
