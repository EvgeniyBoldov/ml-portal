/**
 * Tooltip descriptions for Platform Limits page blocks.
 * Imported by PlatformSettingsPage.
 */

export const GLOBAL_CAPS_TOOLTIP = `Жёсткий потолок — значения нельзя превысить ни одним агентом и ни одной настройкой.
Параметры:
• Макс. таймаут — максимальное время выполнения одного запуска агента (сек)
• Макс. ретраев — предел повторных попыток при ошибке инструмента
• Макс. шагов агента — предел итераций основного цикла агента
• Макс. шагов планировщика — предел шагов внутри одного плана
• Макс. конкурентных операций — одновременных запусков на экземпляр
• Макс. время задачи — суммарный лимит на всю задачу целиком
• Макс. вызовов инструментов на шаг — предел tool_call за один шаг`;

export const EXEC_DEFAULTS_TOOLTIP = `Значения по умолчанию для агентов, у которых параметр не задан явно.
Порядок приоритетов: Агент → Оркестрация (эти значения) → Платформенный cap (потолок выше).
Параметры:
• Макс. шагов — сколько итераций разрешено агенту по умолчанию
• Таймаут — лимит времени одного запуска в секундах
• Макс. попыток — сколько раз повторять вызов инструмента при ошибке`;

export const EXEC_DEFAULTS_BLOCK_TITLE = 'Ограничения по умолчанию';
export const GLOBAL_CAPS_BLOCK_TITLE = 'Глобальные ограничения';
