/**
 * Tooltip descriptions for Platform Limits page blocks.
 * Imported by PlatformSettingsPage.
 */

export const EXEC_DEFAULTS_TOOLTIP = `Лимиты рантайма на уровне платформы.
Применяются как базовые значения, если для конкретной сущности не задан override.
Параметры:
• Runtime шаги — лимит шагов выполнения
• Runtime вызовы инструментов — общий лимит tool-вызовов
• Runtime ретраи — лимит повторных попыток
• Runtime wall time — общий лимит времени выполнения
• Runtime total токены — общий лимит токенов рантайма`;

export const EXEC_DEFAULTS_BLOCK_TITLE = 'Ограничения по умолчанию';
