import { test } from '@playwright/test';

test.describe('Smoke Placeholder', () => {
  test.skip('baseline placeholder', async () => {
    // Keep at least one declared e2e test while the suite is being rebuilt.
  });
});
